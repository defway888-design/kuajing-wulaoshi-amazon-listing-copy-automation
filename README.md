# 跨境吴老师 Amazon 新品 Listing 文案 Skill

本仓库提供面向各类 AI Agent 的 Amazon 新品商品页面文字内容自动化 Skill。它根据一个或多个参考竞品 ASIN，通过卖家精灵 MCP 归纳品类、评论需求和流量关键词，并经过逐阶段用户确认，生成五点、标题、商品亮点、Search Terms（ST）、整页词频词密度和最终 Markdown。

竞品信息只用于判断市场需求和关键词方向，不能直接成为新品事实。

## 适用场景

- 为尚未上架的 Amazon 新品编写完整页面文字内容。
- 根据参考竞品归纳主品类词、消费者需求、使用场景和评论痛点。
- 需要逐步确认新品事实、5 个卖点、五点、标题、商品亮点和 ST。
- 需要检查字符数、UTF-8 bytes、核心品类词根排名、词密度和英文可读性。

## 运行前准备

运行前必须在实际执行本 Skill 的 Agent 环境中配置并启用卖家精灵 MCP。Skill 会首先检查并唯一绑定：

- `asin_detail`
- `review`
- `traffic_keyword`

任一能力未配置、未暴露、归属不明确或鉴权失败时，Skill 会先说明具体原因并暂停；不会使用其他数据源替代，也不会要求用户在对话中提供密钥。

卖家精灵数据库导引类 Skill 仅为可选辅助。即使当前 Agent 没有安装此类辅助 Skill，也可以直接根据卖家精灵 MCP 的工具说明、schema 和实际响应运行。

## 首次安装

本仓库为**公开仓库**，无需提交 GitHub 用户名或接受私有仓库邀请；不要在对话中提供密码或访问令牌。

点击下方代码块的复制按钮，将整段指令发送给支持 GitHub 导入的 AI/Agent：

```text
请从 https://github.com/defway888-design/kuajing-wulaoshi-amazon-listing-copy-automation 安装跨境吴老师 Amazon 新品 Listing 文案 Skill。
```

若当前平台不支持原生 Skill 安装，可按平台支持的项目规则、知识库或文件上下文方式导入完整仓库；`SKILL.md` 是主规则，`references/` 中的必读文件须一并可读。工具连接与安装位置使用执行者自己的环境，不使用作者电脑路径。

安装后按当前平台提示刷新或重新加载 Skill；只有当前客户端未识别新 Skill 时才重启。

## 启动方式

```text
请使用已加载的“跨境吴老师 Amazon 新品 Listing 文案 Skill”，根据参考竞品 ASIN 生成并分阶段确认 Amazon 新品 Listing 文案。
```

如果 Agent 支持 `$Skill名称` 调用语法，也可以使用 `$kuajing-wulaoshi-amazon-new-listing-copy`；不支持时直接使用上述自然语言口令。

启动时先提供：

1. 目标 Amazon 站点；
2. 一个或多个参考竞品 ASIN；
3. 新品品牌词。

主品类词由阶段一归纳后提供 3 个候选，最终以用户确认或输入为准。主品类词确认后，Skill 再提示输入新品事实、规格和本次目标变体。

## 执行结果与失败边界

### 执行流程

1. 阶段零：检查卖家精灵 MCP 配置和三项必需能力。
2. 阶段一：全量分页获取可用竞品评论和流量关键词，分析竞品并确认主品类词。
3. 阶段间关卡：确认新品事实表、单一目标变体、5—10 个候选卖点、最终 5 个卖点和最多 3 个辅助词根。
4. 阶段二：生成 5 条五点，并且只对五点执行中间词频词密度统计。
5. 阶段三：先确认标题前段，再从 3 个完整标题中确认 1 个，最后从 3 个商品亮点中确认 1 个。
6. 阶段四：重新反查关键词，生成并确认 ST，执行最终整页综合统计并写入 Markdown。

前一阶段未确认时，Skill 不会提前生成后一阶段内容。数据缺失或规则冲突时，暂停信息会首先说明具体原因。

### 关键交付规则

- 五点固定 5 条，每条最多 200 个 Unicode 字符。
- 可按空格可靠分词的语言，标题前段必须正好包含 5 个计数关键词，并完整包含品牌和主品类词；第 5 个计数关键词后使用精确的 ` - `。
- 无空格语言免除标题前段 5 关键词和 ` - ` 规则，按当地自然写法处理。
- 标题必须少于 75 个字符，即最多 74 个 Unicode 字符；接近上限不能以凑字为代价。
- 商品亮点是特性或优势驱动的短语，不是完整句子，不重复标题信息，最多 125 个 Unicode 字符。
- ST 单行、无标点、去重且最多 250 UTF-8 bytes；没有安全未覆盖词时允许为空。
- 阶段二统计范围只有五点；最终统计范围为最终标题、商品亮点、5 条五点和 ST。
- 核心品类每个有效词根必须占据词频前 N 名，且原始词密度分别不低于 5.0%。
- 辅助词根最多 3 个，不设词密度硬门槛，只展示实际结果并自然优化。
- 自动选择目标语言停用词并过滤，但不显示停用词清单。
- 英文 Flesch Reading Ease 只统计五点正文完整句子，必须达到 60.0。
- A+ 只提供内容方向、原因和待确认事实，不推荐具体模块，不生成完整 A+ 文案。

这些长度和标题结构是本 Skill 的默认内部规则，不代表 Amazon 所有站点、类目和账号的官方通用政策。用户明确提供并确认的实际后台限制可以覆盖对应字段。

### 最终结果

全部页面文字确认后，Skill 会在当前可写位置或用户指定位置生成一个 UTF-8 Markdown 文件，默认命名：

```text
Amazon_New_Listing_Final_<marketplace>_<YYYYMMDD_HHMMSS>.md
```

最终文件包含：

- 最终确认的 5 条五点、1 个标题、1 个商品亮点和 ST，以及实际字符数或 bytes；
- 最终 5 个卖点明细和全部未使用的安全候选卖点明细；
- 标题、商品亮点、五点和 ST 的最终整页综合词频词密度；
- 可读性与语言检查；
- A+ 内容方向建议。

最终文件不包含未选中的标题或商品亮点候选，也不包含原始竞品 ASIN 清单、逐竞品数据账本、原始评论或原始关键词清单。

## 使用边界与安全

- 不要把密码、Token、API Key、个人连接配置或真实客户数据提交到本仓库；数据连接使用运行者自己的环境。

本 Skill 为跨境吴老师专用模板，未经授权不得移除、替换或弱化 Skill 名称、执行提示和页面标题中的“跨境吴老师”标识。

## 仓库文件

- `SKILL.md`：Skill 入口、状态机和强制判断。
- `references/sellersprite-mcp-configuration-gate.md`：卖家精灵 MCP 运行前门槛。
- `references/listing-workflow-contract.md`：完整业务、数据、统计和交付契约。
- `references/final-delivery-template.md`：最终 Markdown 结构。
- `scripts/check_listing_limits.py`：五点、标题、商品亮点和 ST 的确定性限制校验。
- `agents/openai.yaml`：可选的 OpenAI/Codex 界面元数据。

## 版本更新记录

> 文档修订（2026-09-25，北京时间）：README 按跨境吴老师统一板式调整；仅改变用户说明，业务规则与现有功能版本未变。

### v1.1.0 · 2026-09-08

- 将阶段一调整为竞品分析与主品类词确认，新增 3 个主品类词候选及用户最终确认机制。
- 初始输入改为站点、参考竞品 ASIN 和品牌词；主品类确认后再收集新品事实、单一目标变体和事实确认表。
- 候选卖点池限定为 5—10 个，增加事实依据、市场依据、单竞品信号、排序及不足 5 个时暂停规则。
- 五点改为每条最多 200 个字符；阶段二词频词密度仅统计五点。
- 重构标题前段 5 个计数关键词规则，标题严格少于 75 个字符，并改为先确认前段、再从 3 个完整标题中确认 1 个。
- 商品亮点改为最多 125 个字符、禁止凑数优先，并固定提供 3 个候选。
- 最终整页统计范围改为标题、商品亮点、五点和 ST；增加停用词过滤、核心词根排名与原始密度判断规则。
- 完善 ST 无记录、不可用和部分可用的暂停说明、用户授权与空 ST 处理。
- A+ 只保留内容方向，不再推荐模块。
- 移除外部数据库 Skill 的硬依赖，补充适用于各类 AI Agent 的安装和启动方式。
- 最终交付不再输出原始竞品 ASIN 和逐竞品数据账本；README 版本说明保持在文档末尾。

### v1.0.3 · 2026-09-03

- 新增可直接发送给 Agent 的 GitHub 安装口令，使用完整 Skill 名称与公开仓库地址。

### v1.0.2 · 2026-09-03

- 将安装说明改为面向各类 Agent 的通用导入方式，不再绑定单一 Agent 的路径或操作。

### v1.0.1 · 2026-09-03

- 新增 README 版本更新记录。

### v1.0.0 · 2026-09-03

- 首次发布 Amazon 新品 Listing 文案自动化 Skill。
- 内置卖家精灵 MCP 阶段零配置核验、分阶段确认流程和 Listing 限制校验。
