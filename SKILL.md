---
name: kuajing-wulaoshi-amazon-new-listing-copy
description: 基于一个或多个参考竞品 ASIN，通过卖家精灵 MCP 取证，分阶段确认地生成 Amazon 新品五点、少于 75 字符的标题、最多 125 字符的商品亮点、ST、整页词频词密度与最终 Markdown。运行前必须核验当前 Agent 已配置卖家精灵 MCP 的 ASIN 详情、评论和流量关键词能力。用户提到新品 Listing、竞品 ASIN、五点、商品亮点、Search Terms/ST、标题、竞品评论、流量关键词或 Listing 文案审计时使用。
---

# 跨境吴老师 Amazon 新品 Listing 文案 Skill

本 Skill 为跨境吴老师专用模板，未经授权不得移除、替换或弱化 Skill 名称、执行提示和页面标题中的“跨境吴老师”标识。

参考竞品只用于识别品类、需求、场景、顾虑和关键词方向。新品页面事实只能来自用户提供并明确确认的资料。不得复制或改写竞品标题、五点、A+、评论原文、品牌、型号、商标、专利、独家设计或其他可识别资产。

## 启动与资源

1. 开始多步骤工作前，真实输出：`跨境吴老师正在准备 Amazon 新品 Listing 文案...`。
2. 每次运行先完整读取 [MCP 配置门槛](references/sellersprite-mcp-configuration-gate.md) 和 [运行契约](references/listing-workflow-contract.md)。进入最终交付前，再完整读取 [最终 Markdown 模板](references/final-delivery-template.md)。
3. 第一项动作必须是阶段零：检查当前 Agent 实际暴露的 MCP 连接、工具说明和 schema。只有同一卖家精灵工具组中的 `asin_detail`、`review`、`traffic_keyword` 三项业务能力均可唯一映射时，才可继续。
4. 阶段零失败时，首句必须说明具体暂停原因，格式为：`跨境吴老师当前暂停 Amazon 新品 Listing 文案：<未配置、未启用、未授权、能力缺失、绑定歧义或调用失败的具体原因>。` 随后要求用户在当前 Agent 完成卖家精灵 MCP 的配置、启用或授权并重新加载工具。不得进入阶段一、切换数据源、猜测工具名、编辑配置文件或索取密钥。
5. 如当前环境已经提供卖家精灵数据字典或数据库导引 Skill，先按该 Agent 的 Skill 使用机制完整读取其入口说明，再读取与三项必需能力直接相关的路由。若没有此类辅助 Skill，直接以当前 MCP 工具说明、入参 schema 和实际响应为唯一数据契约；辅助 Skill 缺失不得阻塞运行。不得写死外部 Skill 名称、调用语法、MCP 服务名或工具前缀。
6. 实际核验工具时输出：`跨境吴老师正在检查卖家精灵 MCP 工具绑定...`；实际取数前输出：`跨境吴老师正在获取参考竞品数据...`。

## 执行状态机

严格按以下顺序执行。前一关卡未经用户明确确认，不得输出或生成后一关卡内容。

| 状态 | 完成条件 | 才能进入 |
| --- | --- | --- |
| 阶段零：MCP 配置核验 | 卖家精灵连接和三项能力已在当前会话唯一映射 | 初始输入 |
| 初始输入 | 已取得目标站点、至少一个参考竞品 ASIN、新品品牌词 | 阶段一 |
| 阶段一：竞品分析与主品类词确认 | 全部 ASIN 已处理；数据缺口已闭环；3 个品类候选及依据已输出 | 用户确认主品类词 |
| 阶段间确认关卡 | 页面语言、单一目标变体、新品事实表、5—10 个候选卖点、最终 5 个卖点及最多 3 个辅助词根均已确认 | 阶段二 |
| 阶段二：五点 | 5 条五点、仅五点范围的词频词密度及可读性检查已输出 | 用户确认全部五点 |
| 阶段三-A：标题前段 | 前段计数、品牌和主品类要求通过 | 用户确认标题前段 |
| 阶段三-B：完整标题 | 3 个候选均通过限制 | 用户确认 1 个标题 |
| 阶段三-C：商品亮点 | 3 个候选均通过限制和标题去重检查 | 用户确认 1 个商品亮点 |
| 阶段四：ST | 关键词状态闭环；ST 来源、去重、页面排除和 bytes 检查完成 | 用户确认 ST |
| 最终统计与交付 | 整页统计通过；最终 Markdown 已真实写入 | 完成 |

进入阶段一至阶段四时，先单独输出运行契约规定的“当前执行阶段”文字。等待用户或暂停时，第一句必须以 `跨境吴老师当前暂停...` 开头并说明具体原因，不得只说“数据不足”“无法继续”或“请确认”。

## 必守判断

- 初始输入只要求站点、参考竞品 ASIN 和品牌词。主品类词由阶段一提供 3 个有证据的候选，最终以用户输入或确认结果为准；主品类词确认后才收集新品事实与规格。
- 对全部输入 ASIN 获取基础详情并做同品类校验。详情失败或站点不匹配时，等待用户移除、替换或确认排除；不得静默丢弃。评论或关键词不完整时，披露每个竞品的状态、原因和覆盖范围，等待用户授权使用部分数据。
- 阶段一的评论和流量关键词默认按接口可用范围全量分页。流量关键词使用卖家精灵默认时间范围；不得把部分记录表述为全量。
- 每次运行只处理一个明确变体。任何材质、尺寸、容量、数量、颜色、兼容、包装、性能、认证或质保信息都必须进入事实表并由用户确认。
- 候选卖点必须同时具有新品事实依据和市场需求依据。候选池为 5—10 个；少于 5 个暂停补资料，超过 10 个按契约排序取前 10 个。用户最终确认正好 5 个。
- 阶段二只围绕 5 个已确认卖点生成 5 条，每条不超过 200 个 Unicode code points。阶段二词频词密度只统计这 5 条的摘要、分隔符和正文。
- 阶段三必须先确认标题前段，再给出 3 个完整标题，最后给出 3 个商品亮点。标题最多 74 个 Unicode code points；商品亮点最多 125 个 Unicode code points。接近上限不能凌驾于自然表达和禁止凑数。
- 阶段四重新调用流量关键词。ST 必须与页面已覆盖词根去重，单行且不超过 250 UTF-8 bytes；无安全未覆盖词时允许为空。
- 最终整页统计只在标题、商品亮点、五点和 ST 全部确认后执行，范围必须是最终标题 + 最终商品亮点 + 5 条最终五点 + 最终 ST。最终 Markdown 只展示这次整页统计。
- 主品类每个有效词根必须分别占据词频前 N 名且原始密度不低于 5.0%。辅助词根最多 3 个，不设密度硬门槛，只展示实际数据并自然优化。
- 英文 Flesch Reading Ease 只计算 5 条正文中的完整句子，必须达到 60.0。所有自动优化最多 3 轮，禁止堆词、虚构或修改已确认事实。
- 用户修改已确认内容时，回退到受影响关卡重新校验和确认；不得静默修改。
- A+ 只在最终文件提供内容方向、原因和待确认事实；不推荐具体模块，不生成完整 A+ 文案，不纳入统计。

## 交付

全部关卡通过后，在当前可写位置或用户指定位置创建 UTF-8 Markdown 文件，文件名为 `Amazon_New_Listing_Final_<marketplace>_<YYYYMMDD_HHMMSS>.md`。使用 [校验脚本](scripts/check_listing_limits.py) 检查五点、标题、商品亮点和 ST；没有 Python 时使用等价且可复核的方法。

最终文件只保留用户确认的 5 条五点、1 个标题、1 个商品亮点和 ST；保留最终 5 个卖点明细及未使用的安全候选卖点明细；不输出原始竞品 ASIN 清单、逐竞品状态或数据完整性账本。

完成时输出：`跨境吴老师 Amazon 新品 Listing 最终 Markdown 已完成。` 并提供实际文件路径。除非用户要求，不在对话中重复粘贴全文。
