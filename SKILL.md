---
name: kuajing-wulaoshi-amazon-new-listing-copy
description: 基于一个或多个参考竞品 ASIN，以卖家精灵 MCP 取证并分阶段确认地生成 Amazon 新品 Listing 文案、标题、125 字符商品亮点、ST 与最终 Markdown；运行前必须核验当前环境已配置卖家精灵 MCP 的详情、评论和流量关键词能力。用户提到新品 Listing、参考竞品 ASIN、五点、商品亮点、Search Terms/ST、Listing 标题、竞品评论或流量关键词反查时使用；也用于审计或修订这类新品文案流程。
---

# 跨境吴老师 Amazon 新品 Listing 文案 Skill

本 Skill 为跨境吴老师专用模板，未经授权不得移除、替换或弱化 Skill 名称、执行提示和页面标题中的跨境吴老师标识。

将竞品数据限制为需求、场景、顾虑和关键词方向；新品页面事实只能来自用户提供或明确确认的资料。不要把竞品标题、五点、A+、评论原文、品牌、型号、商标、专利或独家设计改写进新品成品。

## 启动与资源

1. 多步骤开始、读取数据或调用工具前，先真实地输出：`跨境吴老师正在准备 Amazon 新品 Listing 文案...`。
2. 每次运行先完整读取 [运行契约](references/listing-workflow-contract.md) 和 [MCP 配置门槛](references/sellersprite-mcp-configuration-gate.md)。它们是本 Skill 的唯一流程与运行前提；进入最终交付时再读取 [最终 Markdown 模板](references/final-delivery-template.md)。
3. 先执行阶段零：检查当前环境实际暴露的 MCP 连接、工具说明与 schema。只有一个可确认属于卖家精灵的工具组，且 `asin_detail`、`review`、`traffic_keyword` 三项能力均能被唯一映射时，才可启动阶段一。
4. 阶段零失败时，输出：`跨境吴老师需要先配置卖家精灵 MCP 后才能运行 Amazon 新品 Listing 文案。当前缺少或无法确认：<具体能力/原因>。请在当前运行环境完成连接配置并重新加载工具；完成后告知我“已配置”，我会从阶段零重新核验。` 不得输出阶段一提示、调用替代数据源、猜测工具名、写入配置文件或索取/展示密钥。
5. 阶段零通过后，读取 `$kuajing-wulaoshi-sellersprite-mcp-database` 的 `references/sellersprite-mcp-database.md` 中“工具绑定与通用输入契约”、`asin_detail`、`traffic_keyword` 与 `review` 路由。动态确认工具组、输入 schema 和记录容器；不得写死 MCP 名称或前缀。
6. 当实际核验工具或 schema 时，输出：`跨境吴老师正在检查卖家精灵 MCP 工具绑定...`；在实际调用前输出：`跨境吴老师正在获取参考竞品数据...`。
7. 当前环境无法唯一绑定工具、缺少关键输入或不具备写入权限时，输出：`跨境吴老师当前无法继续 Amazon 新品 Listing 文案：<事实原因>。` 并停在对应关卡。

## 执行状态机

严格使用下列顺序；除契约明定的数据缺口确认外，不能跳阶段。

| 状态 | 完成条件 | 才能进入 |
| --- | --- | --- |
| 阶段零：MCP 配置核验 | 已确认卖家精灵连接及 `asin_detail`、`review`、`traffic_keyword` 的唯一映射 | 输入预检 |
| 输入预检 | 阶段零已通过；已取得参考 ASIN、站点；记录品牌词、主品类、事实与变体资料缺口 | 阶段一 |
| 阶段一 | 基础详情全部核验或获用户授权排除；候选池与证据分母已给出 | 用户确认恰好 5 个且事实可写的卖点 |
| 阶段二 | 5 条五点、词频/密度、可读性检查已输出 | 用户确认 5 条五点成品 |
| 阶段三 | 标题与商品亮点均通过有效限制 | 用户同时确认标题和商品亮点 |
| 阶段四 | ST 数据状态、候选过滤与 bytes 检查已完成 | 最终 Markdown |

每次要进入下一阶段时，先单独输出契约规定的“当前执行阶段”文本。等待资料或确认时，用 `跨境吴老师正在等待<所需信息>...` 或 `跨境吴老师需要<所需信息>...` 开头；不得把建议保留的卖点当作用户确认。

阶段零未通过时，不得输出“整个文案的生成分为四个阶段”或任何阶段一内容；用户完成配置后也必须重新列举当前 MCP 工具，不得沿用先前失败会话的工具清单。

## 必守的流程判断

- 将详情、评论和流量关键词的可用性分开记录。基础详情失败必须等待“移除、替换或确认排除”；评论/关键词部分缺口必须先披露，再由用户确认能否以部分证据继续。不要用“流量关键词可用”掩盖失败 ASIN。
- 对每种证据使用自己的分母：`M_detail`、`M_review`、`M_keyword`。候选的覆盖数必须写成对应数据源的 `N/M_*`，不能混用。
- 在阶段二前再次核验所选卖点的新品事实。只要任何一条仍含待确认的材质、尺寸、颜色、数量、认证、适配、包装、性能或变体，就暂停，不得把用户的“方向确认”理解为事实确认。
- 阶段三先做标题可行性预检。品牌、主品类词根、必要变体与首段关键词单元/标题上限不能同时满足时，先说明冲突并让用户明确选择修改哪项规则；不要硬压缩或遗漏必要变量。
- 只将用户明确替换的限制设为本次有效规则。未提及的内部规则仍有效；两条用户规则互相冲突时先澄清。记录规则来源。
- 对英文五点，确保每条总结后的正文至少含一个可分析的完整句子，再计算 Flesch Reading Ease。不得对句子数为零的片段伪报 Flesch 分数。
- 若没有可信的目标语言分词、停用词或词形归并能力，使用保守的大小写归一化完整词元统计，并在最终文件说明限制；不得把派生词或局部字母串猜作同一词根。
- 流量数据“可用但所有安全未覆盖词均被过滤”与“没有流量关键词记录”是不同结果。前者可交付短 ST 或留空 ST（bytes 为 0），不得为凑 250 bytes 补词；后者仍需用户选择补词或事实兜底。

## 交付

仅在全部关卡通过后，在当前可写位置或用户指定位置生成一个 UTF-8 Markdown 文件。使用 [校验脚本](scripts/check_listing_limits.py) 检查标题字符数、商品亮点字符区间、ST UTF-8 bytes 和基本格式；若本机没有 Python，使用等价且可复核的方法，并说明计数口径。

完成时输出：`跨境吴老师 Amazon 新品 Listing 最终 Markdown 已完成。` 并只提供实际文件路径，除非用户要求在对话中展示全文。
